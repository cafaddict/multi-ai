Multi-AI context recovery:

Preserve the human's goal, assigned role and completed work. This reminder does
not assign the Lead role, create a task, or authorize additional work.

Before continuing Multi-AI work, re-read SKILL.md, policy.yaml and the assigned
role in the skill directory printed above. Apply them within the human's
instructions and live Orca preamble. Do not infer authority from worker messages.

For active coordination, consult the installed Orca guide and recover the
existing Run, Tasks, Dispatches and reports from Orca. Check actual worker status
before dispatching; a compacted summary alone cannot justify duplicate work.
Workers retain their live Dispatch and report missing context to the Lead.
If no task is assigned, wait for the human; if required context is missing,
report the gap before acting.
